package com.sysdrill.backend.identity

/** PLAN.md step 35 — platform-wide role, distinct from [com.sysdrill.backend.organization.OrganizationRole] which is scoped to a single organization. */
enum class PlatformRole { USER, PLATFORM_ADMIN }
